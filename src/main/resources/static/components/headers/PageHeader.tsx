import * as React from 'react';

const PageHeader = () => <div className="jumbotron">
    <div className="container">
        <h1 className="display-3">LETSBE09</h1>
        <p className="lead">TypeScript + RxJS + ReactJS + Webpack + Groovy + Spring + Mybatis</p>
    </div>
</div>;

export default PageHeader;